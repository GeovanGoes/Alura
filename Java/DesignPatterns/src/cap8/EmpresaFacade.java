package cap8;

public class EmpresaFacade {
	
	/**
	 * Facade
	 * Prover uma interface para expor todos os servicos da empresa,
	 * Isso evita que o cliente tenha que conhecer todas as interfaces do meu sistema
	 * */
	
	/**
	 * Singleton é usado quando precisamos que um objeto tenha uma única instancia no nosso sistema
	 * */
}
